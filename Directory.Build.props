<Project>
  <PropertyGroup>
    <!-- Default (กันเผื่อ) -->
    <VersionPrefix>0.0.0</VersionPrefix>
    <AssemblyVersion>0.0.0.0</AssemblyVersion>
    <FileVersion>0.0.0.0</FileVersion>
  </PropertyGroup>

  <!-- Develop-A: Major = 1 -->
  <PropertyGroup Condition="'$(GIT_BRANCH)' == 'DevelopRIST'">
    <VersionPrefix>1.0.0</VersionPrefix>
    <AssemblyVersion>1.0.0.0</AssemblyVersion>
    <FileVersion>1.0.$(BUILD_NUMBER).0</FileVersion>
  </PropertyGroup>

  <!-- Develop-B: Major = 2 -->
  <PropertyGroup Condition="'$(GIT_BRANCH)' == 'DevelopREPI'">
    <VersionPrefix>2.0.0</VersionPrefix>
    <AssemblyVersion>2.0.0.0</AssemblyVersion>
    <FileVersion>2.0.$(BUILD_NUMBER).0</FileVersion>
  </PropertyGroup>

 
 
</Project>
